import { BottomTabBarProps } from "@react-navigation/bottom-tabs";
import { Stack } from "native-base";

interface Props extends BottomTabBarProps {}

export const HomeBottomNavigationBar = (props: Props) => {
  return <Stack />;
};
